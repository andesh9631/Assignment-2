//12. Assume price of 1 USD is INR 76.23. Write a
// program to take the amount in INR and convert it
// into USD.
#include<stdio.h>
int main()
{
	float inr,a ,usd;
	printf("Enter the value of a:");
	scanf("%f",&a);
	  inr=a*(1/76.23);
	  printf(" %f ",inr);
	  return 0;
      

	  // convert $ to inr

	//   float a ,usd;
	// printf("Enter the value of a:");
	// scanf("%f",&a);
	//   usd=a*76.32;
	//   printf(" %f ",usd);
	//   return 0;


	  
}